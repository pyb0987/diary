오늘은 뭐 거의 아무것도 못한 날이었다.

다만 유의미한 진전을 꼽자면 어떤식으로 package.json을 관리해야 할지 감이 오는 '것 같은' 것과

내가 딱 원하는 좋은 차트 패키지를 발견한 것이다.

원래는 그냥 구현하려고 했으나, 충분한 시간을 가지고 생각해보니, 반응형으로 만들고, 휠로 움직이고, 기술지표를 계산하고,

또 그것을 나타내고, 선과 다른 그리기 도구를 제공하고 하는 등의 모듈을 개발하려면 그것만으로 시간이 몇 개월은 걸릴 것 같아

이미 있는 바퀴를 다시 발명하지 않기로 했다.

다음은 내가 하루간 씩씩 거리면서 내가 한 일들의 '로그'를 작성한 것이다.


vue bootstrap plugin 찾음
vue에서 바로 api 구축하기 위한 검색
vue에서 ApiUri만드는 전역함수 prototype으로 작성
CORS문제 해결 - node.js의 cors 플러그인
npm에서 차트 찾기 - 구글에서 찾다가 한참걸림
npm ci --only=production으로 의존성 안깨트리고 설치
결국 테스트버전 실험하는 것 reify core-js code 1 에러 등으로 실패
test파일을 frontend 폴더 내에서 돌려보려 했으나 실패
인터넷에서 시키는대로 node-modules을 삭제하고 다시 깔았으나 code1 에러 다시 봉착
webpack-dev-server 작동 안함
npm-check 이용하여 의존성 관리
npm install webpack-dev-server --save-dev 자꾸 하라고 뜸 왜인지 모르겠음
extract-text-webpack-plugin 계속 문제
패키지.json 맨처음 스냅샷 다시 가지고 와서 버전업 안시키고 설치하고 의존성만 유지
드디어 해결
